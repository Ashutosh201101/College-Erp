version: "3.8"

services:
  erp:
    build: .
    container_name: college-erp-app
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=local-dev-secret-key
    volumes:
      - erp_data:/app
    restart: unless-stopped

volumes:
  erp_data:
